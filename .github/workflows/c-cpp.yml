name: C/C++ CI

on: [push]

jobs:
  ubuntu-build:
    runs-on: windows-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2.0.0
    - name: Hello world
      run: |
        echo %PATH%
        dir
      shell: cmd
